version: '3.9'

services:
  game-service:
    container_name: gameservice
    build: .
    ports:
      - "8089:8089"
    networks:
      - mynetwork

networks:
  mynetwork:
    external: true
    name: global-network
    driver: bridge
